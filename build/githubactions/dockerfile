FROM golang:1.17.3-alpine

# RUN wget -q https://dl.google.com/go/go1.14.2.linux-amd64.tar.gz -O - | tar -xz -C /usr/local
# ENV PATH $PATH:/usr/local/go/bin

WORKDIR /keeper-bot

# Build Project
COPY go.mod .
COPY go.sum .
RUN go mod download
COPY . .

